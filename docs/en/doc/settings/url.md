---
title: url
createTime: 2025/11/29 13:08:22
---
------





































































































































































































































































































































































































































































































































































































































































































</style>}  border-left: 4px solid var(--vp-c-tip-1);  background: var(--vp-c-tip-soft);  margin: 2rem 0;  border-radius: 0.75rem;  padding: 1.5rem;.tip-box {}  border-left: 4px solid var(--vp-c-purple-1);  background: var(--vp-c-purple-soft);.theme {}  border-left: 4px solid var(--vp-c-yellow-1);  background: var(--vp-c-yellow-soft);.warning {}  border-left: 4px solid var(--vp-c-blue-1);  background: var(--vp-c-blue-soft);.highlight {}  box-shadow: 0 2px 8px var(--vp-c-shadow-1);  border-color: var(--vp-c-brand-1);.config-card:hover {}  transition: all 0.2s ease;  background: var(--vp-c-bg-soft);  border: 1px solid var(--vp-c-divider);  margin: 1.5rem 0;  border-radius: 0.75rem;  padding: 1.25rem;.config-card {<style></div>```</script>}    openSecRandom('direct_extraction');    // Use quick drawfunction openDirectExtraction() {}    openSecRandom('pumping', 'action=stop');function stopPumping() {}    openSecRandom('pumping', 'action=start');function startPumping() {}    window.location.href = url;    const url = `secrandom://${action}${params ? '?' + params : ''}`;function openSecRandom(action, params = '') {<script><button onclick="openDirectExtraction()">Use Quick Draw</button><button onclick="stopPumping()">Stop Person Selection</button><button onclick="startPumping()">Start Person Selection</button><button onclick="openSecRandom('main')">Open SecRandom</button><!-- Web page integration example -->```html#### Web Application Integration```Invoke-SecRandom -Action "direct_extraction"Invoke-SecRandom -Action "settings"Invoke-SecRandom -Action "pumping" -Parameters "action=start"Invoke-SecRandom -Action "main"# Usage example}    Start-Process $url        }        $url += "?$Parameters"    if ($Parameters) {    $url = "secrandom://$Action"        )        [string]$Parameters = ""                [string]$Action,        [Parameter(Mandatory=$true)]    param(function Invoke-SecRandom {# PowerShell integration example```powershell#### PowerShell Script```pauseecho Demonstration complete!timeout /t 2 >nulstart secrandom://direct_extractionecho 6. Use quick drawtimeout /t 2 >nulstart secrandom://settingsecho 5. Open settings interfacetimeout /t 2 >nulstart secrandom://pumping?action=stopecho 4. Stop person selectiontimeout /t 5 >nulstart secrandom://pumping?action=startecho 3. Start person selectiontimeout /t 2 >nulstart secrandom://pumpingecho 2. Switch to person extraction interfacetimeout /t 2 >nulstart secrandom://mainecho 1. Open main interfaceecho ========================echo SecRandom Automation Demonstration:: Automation demonstration script@echo off```batch#### Batch Automation<div class="config-card warning">### 🔧 Advanced Integration Techniques</div>```End Sub    MsgBox "Random selection complete!", vbInformation        shell.Run "secrandom://pumping?action=stop"    Application.Wait Now + TimeValue("0:00:05")    ' Auto-stop after 5 seconds        shell.Run "secrandom://pumping?action=start"    ' Start person selection        Set shell = CreateObject("WScript.Shell")    Dim shell As ObjectSub StartRandomSelection()' Excel VBA integration example```vba**Office Add-in + SecRandom**#### Office Software Integration```};    }        await controller.openMain();    async openRewardSystem() {        },        }, 10000);            console.log("Speaker selection complete!");            await controller.stopPumping();        setTimeout(async () => {        // Auto-stop after 10 seconds                await controller.startPumping();        console.log("Randomly selecting speaker...");    async selectSpeaker() {const MeetingSystem = {// Integration example in meeting system```javascript**Meeting System + SecRandom**#### Meeting Software Integration```        self.secrandom.open_settings()        """Open class settings"""    def open_class_settings(self):            print("Selection complete!")        self.secrandom.stop_pumping()        time.sleep(5)  # Run for 5 seconds        self.secrandom.start_pumping()        print("Starting random student selection...")        """Start random student selection"""    def start_random_selection(self):            self.secrandom = SecRandomController()    def __init__(self):class ClassroomManager:from secrandom_controller import SecRandomControllerimport time# Integration example in classroom management system```python**Classroom Management System + SecRandom**#### Educational Software Integration<div class="config-card theme">### 🎯 Practical Application Scenarios</div>```controller.openMain();SecRandomController controller = new SecRandomController();// Usage example}    }        }            e.printStackTrace();        } catch (Exception e) {            Desktop.getDesktop().browse(new URI(BASE_URL + "direct_extraction"));        try {        // Use quick draw    public void openDirectExtraction() {    }        }            e.printStackTrace();        } catch (Exception e) {            Desktop.getDesktop().browse(new URI(BASE_URL + "settings"));        try {    public void openSettings() {    }        }            e.printStackTrace();        } catch (Exception e) {            Desktop.getDesktop().browse(new URI(BASE_URL + "pumping?action=stop"));        try {    public void stopPumping() {        }        }            e.printStackTrace();        } catch (Exception e) {            Desktop.getDesktop().browse(new URI(BASE_URL + "pumping?action=start"));        try {    public void startPumping() {        }        }            e.printStackTrace();        } catch (Exception e) {            Desktop.getDesktop().browse(new URI(BASE_URL + "main"));        try {    public void openMain() {        private static final String BASE_URL = "secrandom://";class SecRandomController {import java.net.URI;import java.awt.Desktop;```java#### Java Integration```controller.OpenMain();var controller = new SecRandomController();// Usage example}    }        });            UseShellExecute = true            FileName = BaseUrl + "direct_extraction",        {        Process.Start(new ProcessStartInfo        // Use quick draw    {    public void OpenDirectExtraction()    }        });            UseShellExecute = true            FileName = BaseUrl + "settings",        {        Process.Start(new ProcessStartInfo    {    public void OpenSettings()        }        });            UseShellExecute = true            FileName = BaseUrl + "pumping?action=stop",        {        Process.Start(new ProcessStartInfo    {    public void StopPumping()        }        });            UseShellExecute = true            FileName = BaseUrl + "pumping?action=start",        {        Process.Start(new ProcessStartInfo    {    public void StartPumping()        }        });            UseShellExecute = true            FileName = BaseUrl + "main",        {        Process.Start(new ProcessStartInfo    {    public void OpenMain()        private const string BaseUrl = "secrandom://";{class SecRandomControllerusing System.Diagnostics;using System;```csharp#### C# Integration```controller.openMain().catch(console.error);const controller = new SecRandomController();// Usage example}    }        });            });                else resolve();                if (error) reject(error);            exec(`start ${this.baseUrl}direct_extraction`, (error) => {        return new Promise((resolve, reject) => {        // Use quick draw    async openDirectExtraction() {    }        });            });                else resolve();                if (error) reject(error);            exec(`start ${this.baseUrl}pumping?action=stop`, (error) => {        return new Promise((resolve, reject) => {    async stopPumping() {        }        });            });                else resolve();                if (error) reject(error);            exec(`start ${this.baseUrl}pumping?action=start`, (error) => {        return new Promise((resolve, reject) => {    async startPumping() {        }        });            });                else resolve();                if (error) reject(error);            exec(`start ${this.baseUrl}main`, (error) => {        return new Promise((resolve, reject) => {    async openMain() {        }        this.baseUrl = "secrandom://";    constructor() {class SecRandomController {const { exec } = require('child_process');```javascript#### JavaScript/Node.js Integration```controller.open_main()controller = SecRandomController()# Usage example        os.system(f"start {self.base_url}direct_extraction")        """Use quick draw"""    def open_direct_extraction(self):        os.system(f"start {self.base_url}settings")        """Open settings interface"""    def open_settings(self):            os.system(f"start {self.base_url}pumping?action=stop")        """Stop person selection"""    def stop_pumping(self):            os.system(f"start {self.base_url}pumping?action=start")        """Start person selection"""    def start_pumping(self):            os.system(f"start {self.base_url}main")        """Open main interface"""    def open_main(self):            self.base_url = "secrandom://"    def __init__(self):class SecRandomController:import osimport subprocess```python#### Python Integration### 💻 Programming Language Integration Examples<div class="config-card highlight">## 🤝 Software Integration</div>- Check software logs for detailed error messages- Check if software version supports the action- Confirm parameter spelling is correct**Action Parameter Invalid**- If above factors are excluded, report bug to developer- Check if system security software blocks protocol calls- Reinstall software to repair protocol registration- Check if SecRandom is properly installed**URL Protocol Won't Open**### 🔧 Troubleshooting4. 📝 **Error Handling**: Recommended to add error handling when using URL protocols3. 🔄 **Parameter Validation**: Ensure action parameter values are correct2. 🛡️ **Security Consideration**: Only use URL protocols listed in official documentation1. 🔐 **Protocol Registration**: Ensure SecRandom correctly registers URL protocol### ⚠️ Important Notes```subprocess.run(['start', 'secrandom://settings'])import subprocess# Python example```python**Third-party Application Calling**```start secrandom://pumping?action=startecho Starting SecRandom person selection function...@echo off```batch**Batch Script**3. Name the shortcut and complete creation2. Enter URL protocol (e.g., `secrandom://main`)1. Right-click desktop → New → Shortcut**Shortcut Creation**### 🎯 Recommended Usage Methods<div class="tip-box">## 💡 URL Protocol Usage Guide</div>```start secrandom://direct_extraction# Batch file example```batch**Usage Example**:- **Function**: Auto switch to quick draw interface and start selection, auto-closes after set time- **URL**: `secrandom://direct_extraction`#### Use Quick Draw<div class="config-card highlight">### Quick Draw Operations</div>- External tools calling plugin functions- Batch configure plugin settings- Quick access to plugin management interface**Use Cases**:- **Function**: Open plugin page for managing and configuring plugins- **URL**: `secrandom://plugin_settings?action=open`#### Open Plugin Page<div class="config-card">### Plugin Settings Operations</div>- Support software development- View project contributor information- Quick access to donation page**Use Cases**:- **Function**: Auto switch to about interface and open contributor dialog- **URL**: `secrandom://about?action=contributor`#### Open Contributor Dialog- **Function**: Auto switch to about interface and open donation dialog- **URL**: `secrandom://about?action=donation`#### Open Donation Dialog<div class="config-card theme">### About Interface Operations</div>```start secrandom://reward?action=stoptimeout /t 10start secrandom://reward?action=start# Prize drawing automation script```batch**Usage Example**:- **Function**: Auto switch to prize drawing interface and clear current drawing results- **URL**: `secrandom://reward?action=reset`#### Reset Prize Drawing- **Function**: Auto switch to prize drawing interface and stop current drawing- **URL**: `secrandom://reward?action=stop`#### Stop Prize Drawing- **Function**: Auto switch to prize drawing interface and start drawing- **URL**: `secrandom://reward?action=start`#### Start Prize Drawing<div class="config-card warning">### Prize Drawing Operations</div>```start secrandom://pumping?action=stoptimeout /t 5start secrandom://pumping?action=start# Batch file example```batch**Usage Example**:- **Function**: Auto switch to person extraction interface and clear current selection results- **URL**: `secrandom://pumping?action=reset`#### Reset Person Selection- **Function**: Auto switch to person extraction interface and stop current selection- **URL**: `secrandom://pumping?action=stop`#### Stop Person Selection- **Function**: Auto switch to person extraction interface and start selection- **URL**: `secrandom://pumping?action=start`#### Start Person Selection<div class="config-card highlight">### Person Selection Operations## URLs with Action Parameters</div>- Activity quick draw completion and auto-close- Classroom or meeting quick random selection- Need quick single draw operation**Use Cases**:- **Function**: Open quick draw interface for rapid selection operations, auto-closes after set time (adjustable in settings)- **URL**: `secrandom://direct_extraction`<div class="config-card">### 8. Quick Draw Interface</div>- Learn about software authors and contributors- Get technical support information- View software version information**Use Cases**:- **Function**: Open about interface to view version and author information- **URL**: `secrandom://about`<div class="config-card">### 7. About Interface</div>- Floating window mode for presentations- Keep floating window visible during multitasking- Quick floating window toggle**Use Cases**:- **Function**: Open floating window interface, toggle floating window display- **URL**: `secrandom://floating`<div class="config-card">### 6. Floating Window Interface</div>- Export historical records- Analyze historical data statistics- View historical selection records**Use Cases**:- **Function**: Open history interface to view past records, auto-load data- **URL**: `secrandom://history`<div class="config-card">### 5. History Records Interface</div>- Marketing activity raffle- Company annual meeting raffle- Activity raffle section**Use Cases**:- **Function**: Open prize drawing interface for raffle activities- **URL**: `secrandom://reward`<div class="config-card">### 4. Prize Drawing Interface</div>- Activity participant random selection- Random speaker selection in meetings- Quick classroom person selection**Use Cases**:- **Function**: Open person extraction interface for random selection- **URL**: `secrandom://pumping`<div class="config-card">### 3. Person Extraction Interface</div>- External tools calling settings interface- Pre-configure settings during batch deployment- Quick access to settings options**Use Cases**:- **Function**: Open settings interface for users to adjust various settings- **URL**: `secrandom://settings`<div class="config-card">### 2. Settings Interface</div>- Other programs calling SecRandom main function- Desktop shortcut direct access to main interface- Quick launch SecRandom from external applications**Use Cases**:- **Function**: Open SecRandom main interface, automatically show and activate window- **URL**: `secrandom://main`<div class="config-card">### 1. Main Interface## Basic Interface URLs> This page provides a complete list of URL protocols supported by SecRandom software. Through these URLs, you can quickly access various modules and execute specific operations.> > 🔗 **SecRandom URL Protocol Reference**# SecRandom URL Protocol Complete Reference---createTime: 2025/11/29 13:02:57title: urltitle: url
createTime: 2025/11/29 13:08:22
---
