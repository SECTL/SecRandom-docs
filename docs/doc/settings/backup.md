---
title: 备份设置
createTime: 2026/08/14 10:00:00
---

# 备份设置（通用）

::: tip 版本提示
本文档对应 **v3** 的"设置 → 通用 → 备份"页面。v3 处于 Alpha 阶段，设置项可能随版本调整，请以实际界面为准。
:::

> **数据无忧** - 通过自动/手动备份与一键恢复，保护你的名单、历史与配置数据。

## 自动备份

### 自动备份
**功能说明**：是否启用自动备份

- 开启：按设定的间隔自动创建备份
- 关闭：不自动备份（仍可手动备份）

### 自动备份间隔
**功能说明**：设置自动备份间隔（天）

### 自动备份上限
**功能说明**：设置自动备份最多保留数量（个），0 为不限制

**注意事项**：超过上限时，最早的备份会被自动清理

## 备份内容

选择需要包含在备份文件中的数据：

| 内容 | 说明 |
|------|------|
| **配置文件** | 软件设置与配置文件（config） |
| **名单数据** | 点名/抽奖名单等数据（list） |
| **历史记录** | 抽取历史记录数据（history） |
| **抽取证明文件** | 可复现和核验抽取结果的证明文件（proofs） |
| **音频资源** | 音频资源文件（audio） |
| **CSES 联动** | CSES 相关联动数据（CSES） |
| **图片资源** | 成员/奖品图片资源（images） |
| **主题资源** | 已安装的主题资源（theme） |
| **运行日志** | 程序运行日志，可能包含敏感信息（logs） |

::: warning 注意
备份不会包含密码等安全信息。安全凭据存储在独立的加密凭据库中，不随备份导出。
:::

## 手动操作

### 立即备份
点击按钮立即创建一份全量备份。

### 查看备份文件
打开备份文件夹，查看备份文件列表（文件名、创建时间、大小）。

### 恢复备份
1. 在备份列表中选择要恢复的备份
2. 点击 **"恢复"**
3. 确认后执行恢复

**注意事项**：
- 恢复会覆盖当前对应数据
- 恢复前会自动创建一份当前数据快照
- 恢复完成后需要**重启应用**生效
- 只接受 SecRandom v3 生成的备份文件

### 删除备份
选择备份后点击 **"删除"**，此操作无法撤销。

## 备份占用

显示备份文件夹当前占用大小，便于评估存储使用情况。

## 相关页面

- 数据导入/导出：见 [名单管理](/doc/settings/listmg)
- 安全凭据：见 [安全设置](/doc/settings/security)
